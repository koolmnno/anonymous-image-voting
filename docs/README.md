# GitHub Pages 静态版

这个目录用于 GitHub Pages 发布。

## 重要限制

GitHub Pages 是静态托管，没有后端数据库，也不能保存用户上传文件到服务器。

因此此版本采用浏览器 `localStorage` 保存：

- 上传的图片
- 投票数据
- 匿名身份

这意味着数据只保存在当前访问者的浏览器里，不同用户之间不会共享图片和投票结果。

如果需要真实多人共享图片和投票，请使用：

- uniCloud
- 微信云开发
- CloudBase
- 云服务器
